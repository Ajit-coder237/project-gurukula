{
  "name": "@tutoros/ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly",
    "test": "vitest run",
    "lint": "echo 'no lint for ui'"
  },
  "dependencies": {
    "clsx": "^2.1.1"
  },
  "devDependencies": {
    "vitest": "^2.1.1"
  }
}
